using System;

namespace TextSection {

  class Program {

    static void Main(string[] args) {

      // var fullName = "Yoga Utomo ";

      // Console.WriteLine($"Trim: '{fullName.Trim()}'");
      // Console.WriteLine($"ToUpper: '{fullName.Trim().ToUpper()}'");

      // var index = fullName.IndexOf(' ');
      // var firstName = fullName.Substring(0, index);
      // var lastName = fullName.Substring(index + 1);
      // Console.WriteLine("First Name: " + firstName);
      // Console.WriteLine("Last Name: " + lastName);

      // var splitted = fullName.Split(' ');
      // Console.WriteLine(splitted[0]);
      // Console.WriteLine(splitted[1]);

      // var newString = fullName.Replace("Yoga", "Agoy");
      // var newString1 = fullName.Replace("o", "e");

      // Console.WriteLine(newString);
      // Console.WriteLine(newString1);

      // if (String.IsNullOrWhiteSpace(" ")) {
      //   Console.WriteLine("Invalid argument");
      // }

      // var stringNumber = "25";
      // var converted = Convert.ToByte(stringNumber);
      // Console.WriteLine(converted);

      // float price = 29.95f;
      // var convertedPrice = price.ToString("C0");
      // Console.WriteLine(convertedPrice);

      // StringBuilderSection.Go();

      // Challenges.IsConsecutive();
      // Challenges.IsDuplicate();
      // Challenges.TimeValidation();
      // Challenges.camelCaseConverter();
      Challenges.checkVowels();
    }
  }
}
